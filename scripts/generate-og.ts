/**
 * Optional: Generate OG images per app.
 * Skipped for now — requires heavy image dependencies (sharp, canvas, etc.).
 * The site falls back to the default icon-512.png for OG images.
 */
console.log('[generate-og] OG image generation is not yet implemented.');
console.log('[generate-og] Using default icon as OG image fallback.');
